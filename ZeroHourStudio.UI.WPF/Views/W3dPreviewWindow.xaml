<Window x:Class="ZeroHourStudio.UI.WPF.Views.W3dPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🎮 عارض نماذج W3D"
        Width="600" Height="500"
        WindowStartupLocation="CenterOwner"
        Background="#0D1117">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="🎮 عارض نماذج W3D" FontSize="20" FontWeight="Bold" Foreground="#00D4FF"/>
            <TextBlock x:Name="FileNameText" Text="" FontSize="12" Foreground="#8899AA" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Summary Stats -->
        <Border Grid.Row="1" CornerRadius="8" Background="#1A2332" Padding="12,8" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock x:Name="MeshCountText" Text="0" FontSize="20" FontWeight="Bold"
                               Foreground="#00D4FF" HorizontalAlignment="Center"/>
                    <TextBlock Text="أجزاء" FontSize="10" Foreground="#556677" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock x:Name="VertexCountText" Text="0" FontSize="20" FontWeight="Bold"
                               Foreground="#00FF88" HorizontalAlignment="Center"/>
                    <TextBlock Text="نقاط" FontSize="10" Foreground="#556677" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock x:Name="FaceCountText" Text="0" FontSize="20" FontWeight="Bold"
                               Foreground="#FFD700" HorizontalAlignment="Center"/>
                    <TextBlock Text="مثلثات" FontSize="10" Foreground="#556677" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <TextBlock x:Name="TextureCountText" Text="0" FontSize="20" FontWeight="Bold"
                               Foreground="#FF69B4" HorizontalAlignment="Center"/>
                    <TextBlock Text="نسيج" FontSize="10" Foreground="#556677" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Meshes Section -->
        <TextBlock Grid.Row="2" Text="🔷 قائمة الأجزاء (Meshes)" FontSize="13" FontWeight="SemiBold"
                   Foreground="#E8EDF3" Margin="0,0,0,6"/>

        <ListBox Grid.Row="3" x:Name="DetailsList"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="Margin" Value="0,1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#161B22"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Error/Empty State -->
        <TextBlock Grid.Row="3" x:Name="ErrorText"
                   FontSize="13" Foreground="#FF6666"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextWrapping="Wrap" Visibility="Collapsed"/>

        <!-- Close -->
        <Button Grid.Row="4" Content="إغلاق" Click="Close_Click"
                Background="#21262D" Foreground="#C9D1D9" BorderBrush="#30363D"
                Padding="20,8" HorizontalAlignment="Right" Margin="0,12,0,0"
                FontSize="12" Cursor="Hand"/>
    </Grid>
</Window>
