<Window x:Class="ZeroHourStudio.UI.WPF.Views.ProfileManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📁 ملفات التعريف السريعة" Width="520" Height="420"
        WindowStartupLocation="CenterOwner" Background="#1E1E2E" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="احفظ أو حمّل مسارات المصدر والهدف والفصيل بنقرة واحدة"
                   Foreground="#00CC66" FontSize="14" Margin="0,0,0,12"/>
        <ListBox Grid.Row="1" x:Name="ProfilesList" Background="#252538" BorderBrush="#333348"
                 BorderThickness="1" Margin="0,0,0,8"
                 SelectionChanged="ProfilesList_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4,2">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#FFD700"/>
                        <TextBlock FontSize="10" Foreground="#888" TextTrimming="CharacterEllipsis">
                            <Run Text="من: "/><Run Text="{Binding SourceModPath}"/>
                            <Run Text=" → "/><Run Text="{Binding TargetModPath}"/>
                            <Run Text=" | "/><Run Text="{Binding TargetFaction}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Border Grid.Row="2" Background="#252538" CornerRadius="6" Padding="10" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="اسم الملف الجديد:" Foreground="#AAA" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" x:Name="NewProfileName" Background="#2A2A3A" Foreground="White"
                         BorderBrush="#444466" Padding="6,4"/>
            </Grid>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button x:Name="SaveCurrentBtn" Content="💾 حفظ الحالي" Click="SaveCurrent_Click" Width="120" Height="34"
                    Background="#00884C" Foreground="White" BorderBrush="#00CC66" Cursor="Hand" Margin="0,0,8,0"/>
            <Button x:Name="LoadSelectedBtn" Content="📂 تحميل المحدد" Click="LoadSelected_Click" Width="130" Height="34"
                    Background="#2A4A5A" Foreground="White" BorderBrush="#4488AA" Cursor="Hand" Margin="0,0,8,0"
                    IsEnabled="False"/>
            <Button Content="إغلاق" Click="Close_Click" Width="80" Height="34"
                    Background="#3A3A4A" Foreground="White" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
