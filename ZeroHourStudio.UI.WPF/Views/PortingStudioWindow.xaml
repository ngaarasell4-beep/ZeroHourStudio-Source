<Window x:Class="ZeroHourStudio.UI.WPF.Views.PortingStudioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ZeroHourStudio.UI.WPF.Views"
        xmlns:root="clr-namespace:ZeroHourStudio.UI.WPF"
        xmlns:vm="clr-namespace:ZeroHourStudio.UI.WPF.ViewModels"
        xmlns:conv="clr-namespace:ZeroHourStudio.UI.WPF.Converters"
        Title="ZeroHour Porting Studio v3.0"
        Width="1400" Height="900"
        MinWidth="1200" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="#0A0E17">

    <!-- WindowChrome: يسمح بالسحب والتحجيم بدون شريط عنوان النظام -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="50" ResizeBorderThickness="6" GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <root:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <root:StringNotEmptyToVisibilityConverter x:Key="StringToVis"/>
        <conv:FactionToAccentColorConverter x:Key="FactionToColor"/>
        <conv:AssetStatusToColorConverter x:Key="StatusToColor"/>
        <conv:DependencyTypeToIconConverter x:Key="TypeToIcon"/>

        <!-- DropZone state triggers -->
        <Style x:Key="DropZoneBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#1A00D4FF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#08FFFFFF" Offset="0"/>
                        <GradientStop Color="#03FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding DropZoneState}" Value="DragOver">
                    <Setter Property="BorderBrush" Value="#00D4FF"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#00D4FF" BlurRadius="20" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding DropZoneState}" Value="Analyzing">
                    <Setter Property="BorderBrush" Value="#FF6B00"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#FF6B00" BlurRadius="15" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding DropZoneState}" Value="Ready">
                    <Setter Property="BorderBrush" Value="#00FF88"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#00FF88" BlurRadius="20" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding DropZoneState}" Value="Transferring">
                    <Setter Property="BorderBrush" Value="#FF3366"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#FF3366" BlurRadius="25" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <!-- === Top Bar === -->
        <Border Grid.Row="0" Background="#0D1117" BorderBrush="#1A2332" BorderThickness="0,0,0,1">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⬡" FontSize="24" Foreground="#00D4FF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="ZEROHOUR" FontSize="16" FontWeight="Bold" Foreground="#E8EDF3" VerticalAlignment="Center"/>
                    <TextBlock Text=" PORTING STUDIO" FontSize="16" FontWeight="Light" Foreground="#00D4FF" VerticalAlignment="Center"/>
                    <Border Background="#1A2332" CornerRadius="4" Padding="6,2" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="v3.0" FontSize="10" Foreground="#00D4FF"/>
                    </Border>
                </StackPanel>

                <!-- Status -->
                <TextBlock Grid.Column="1" Text="{Binding StatusMessage}"
                           Foreground="#8899AA" FontSize="12"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Advanced Tools + CSF Editor Toggle -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- أدوات متقدمة -->
                    <Button Content="🔍 الفروقات"
                            Command="{Binding PreviewDiffCommand}"
                            Background="Transparent" Foreground="#00FF88"
                            BorderBrush="#00FF88" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,4,0"
                            ToolTip="معاينة الفروقات بين المود المصدر والهدف"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="📊 التوازن"
                            Command="{Binding BalanceReportCommand}"
                            Background="Transparent" Foreground="#FFD700"
                            BorderBrush="#FFD700" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,4,0"
                            ToolTip="تقرير توازن الوحدة المحددة"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="🗺️ المراجع"
                            Command="{Binding CrossReferenceMapCommand}"
                            Background="Transparent" Foreground="#FF69B4"
                            BorderBrush="#FF69B4" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,4,0"
                            ToolTip="خريطة المراجع التقاطعية"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="📋 القوالب"
                            Command="{Binding ManageTemplatesCommand}"
                            Background="Transparent" Foreground="#87CEEB"
                            BorderBrush="#87CEEB" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,4,0"
                            ToolTip="إدارة قوالب النقل"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="📁 ملف تعريف"
                            Command="{Binding ManageProfilesCommand}"
                            Background="Transparent" Foreground="#98FB98"
                            BorderBrush="#98FB98" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,4,0"
                            ToolTip="حفظ أو تحميل مسارات المصدر والهدف والفصيل"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="🔄 الفصيل"
                            Command="{Binding FactionConversionCommand}"
                            Background="Transparent" Foreground="#DDA0DD"
                            BorderBrush="#DDA0DD" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,4,0"
                            ToolTip="تحويل الوحدة لفصيل آخر"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="✓ فحص INI"
                            Command="{Binding ValidateIniCommand}"
                            Background="Transparent" Foreground="#FF4500"
                            BorderBrush="#FF4500" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,8,0"
                            ToolTip="فحص ملفات INI للكشف عن أقواس End المفقودة والأسماء المكررة"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="CSF"
                            Command="{Binding ToggleCsfEditorCommand}"
                            Background="Transparent" Foreground="#FF6B00"
                            BorderBrush="#FF6B00" BorderThickness="1"
                            Padding="12,4" FontSize="11" Cursor="Hand"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>

                    <!-- Window Control Buttons -->
                    <Button x:Name="MinimizeBtn" Content="─" Click="Minimize_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Background="Transparent" Foreground="#8899AA"
                            BorderThickness="0" FontSize="14" Width="40" Height="36"
                            Cursor="Hand" Margin="16,0,0,0" ToolTip="تصغير">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1A2332"/>
                                        <Setter Property="Foreground" Value="#E8EDF3"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="MaximizeBtn" Content="☐" Click="Maximize_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Background="Transparent" Foreground="#8899AA"
                            BorderThickness="0" FontSize="14" Width="40" Height="36"
                            Cursor="Hand" ToolTip="تكبير">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1A2332"/>
                                        <Setter Property="Foreground" Value="#E8EDF3"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="CloseBtn" Content="✕" Click="Close_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Background="Transparent" Foreground="#8899AA"
                            BorderThickness="0" FontSize="14" Width="40" Height="36"
                            Cursor="Hand" ToolTip="إغلاق">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF3366"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- === Main Content: 3-Column Layout === -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" MinWidth="300"/>
                <ColumnDefinition Width="*" MinWidth="200"/>
                <ColumnDefinition Width="380" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Source Pane -->
            <local:SourcePaneView Grid.Column="0" DataContext="{Binding SourcePane}" Margin="0,0,4,0"/>

            <!-- Center: Drop Zone + Unit Preview + Dependencies -->
            <Grid Grid.Column="1" Margin="4,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" MinHeight="100"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Drop Zone -->
                <local:DropZoneView Grid.Row="0" Margin="0,0,0,4"/>

                <!-- Cameo/Texture Preview Card -->
                <Border Grid.Row="1" CornerRadius="8" Background="#0D1117"
                        BorderBrush="#1A2332" BorderThickness="1" Margin="0,0,0,4"
                        Visibility="{Binding HasSelectedUnit, Converter={StaticResource BoolToVis}}">
                    <Grid Margin="12,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Large Cameo Image -->
                        <Border Grid.Column="0" Width="64" Height="64" CornerRadius="6"
                                Background="#161B22" Margin="0,0,12,0"
                                BorderBrush="#21262D" BorderThickness="1">
                            <Image Source="{Binding SelectedUnitPreviewImage}"
                                   Stretch="Uniform" Margin="3"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>

                        <!-- Unit Info -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding SelectedUnitName}" FontSize="15"
                                       FontWeight="Bold" Foreground="#E8EDF3"/>
                            <TextBlock Text="{Binding SelectedUnitInfo}" FontSize="11"
                                       Foreground="#8899AA" Margin="0,3,0,0"
                                       TextWrapping="Wrap"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0"
                                        Visibility="{Binding SelectedUnitModel, Converter={StaticResource StringToVis}}">
                                <TextBlock Text="{Binding SelectedUnitModel}" FontSize="10"
                                           Foreground="#556677" VerticalAlignment="Center"/>
                                <Button Content="🎮 W3D" Command="{Binding W3dPreviewCommand}"
                                        Background="Transparent" Foreground="#00D4FF"
                                        BorderBrush="#00D4FF" BorderThickness="1"
                                        Padding="6,1" FontSize="9" Cursor="Hand"
                                        Margin="8,0,0,0" VerticalAlignment="Center"
                                        ToolTip="عرض معلومات النموذج ثلاثي الأبعاد"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Dependency Graph -->
                <local:DependencyGraphView Grid.Row="2" DataContext="{Binding DependencyGraph}"/>
            </Grid>

            <!-- Right: Target Pane -->
            <local:TargetPaneView Grid.Column="2" DataContext="{Binding TargetPane}" Margin="4,0,0,0"/>
        </Grid>

        <!-- === Bottom Bar === -->
        <Border Grid.Row="2" Background="#0D1117" BorderBrush="#1A2332" BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Transfer Button -->
                <Button Grid.Column="0" Content="⚡ نقل الوحدة المحددة"
                        Command="{Binding TransferSelectedCommand}"
                        Background="#00D4FF" Foreground="#0A0E17"
                        FontWeight="Bold" FontSize="12"
                        Padding="20,6" HorizontalAlignment="Left"
                        Cursor="Hand" BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#00D4FF" BlurRadius="15" ShadowDepth="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Transfer Status -->
                <TextBlock Grid.Column="1" Text="{Binding StatusMessage}"
                           Foreground="#556677" FontSize="11"
                           VerticalAlignment="Center" Margin="0,0,12,0"/>

                <!-- Progress Bar -->
                <Grid Grid.Column="2" VerticalAlignment="Center">
                    <ProgressBar Value="{Binding ProgressValue}" Maximum="100" Height="6"
                                 Background="#1A2332" Foreground="#00D4FF" BorderThickness="0"/>
                    <TextBlock Text="{Binding ProgressValue, StringFormat={}{0}%}"
                               Foreground="#8899AA" FontSize="10"
                               HorizontalAlignment="Right" Margin="0,-14,0,0"/>
                </Grid>

                <!-- Export/Import Log -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Button Content="📤 تصدير السجل" Command="{Binding ExportTransferLogCommand}"
                            Background="Transparent" Foreground="#8899AA" BorderBrush="#556677" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" Margin="0,0,4,0" ToolTip="تصدير سجل النقل إلى JSON"/>
                    <Button Content="📥 استيراد" Command="{Binding ImportTransferLogCommand}"
                            Background="Transparent" Foreground="#8899AA" BorderBrush="#556677" BorderThickness="1"
                            Padding="8,4" FontSize="10" Cursor="Hand" ToolTip="استيراد سجل من ملف"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- === Conflict Resolution Overlay === -->
        <local:ConflictResolutionDialog Grid.RowSpan="3"
            DataContext="{Binding ConflictResolution}"
            Visibility="{Binding DataContext.ShowConflictDialog, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}"/>

        <!-- === Loading Overlay === -->
        <Border Grid.RowSpan="3" Background="#CC0A0E17"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Background="#1A2332" Foreground="#00D4FF" BorderThickness="0"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#00D4FF"
                           FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
