<Window x:Class="ZeroHourStudio.UI.WPF.Views.MergeConflictResolverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🧬 الدمج الذكي (Smart Merge)"
        Width="800" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E"
        Foreground="White"
        FontFamily="Segoe UI">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="🧬 الدمج الذكي" FontSize="20" FontWeight="Bold" Foreground="#00CC66"/>
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="المصدر: " Foreground="#AAAAAA"/>
                <TextBlock Text="{Binding SourceName, Mode=OneWay}" Foreground="#87CEEB" FontWeight="SemiBold" Margin="0,0,20,0"/>
                <TextBlock Text="الهدف: " Foreground="#AAAAAA"/>
                <TextBlock Text="{Binding TargetName, Mode=OneWay}" Foreground="#FFD700" FontWeight="SemiBold"/>
            </StackPanel>
        </StackPanel>

        <!-- Strategy Selector -->
        <Border Grid.Row="1" Background="#252538" CornerRadius="6" Padding="10,6" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="الاستراتيجية:" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <RadioButton Content="تفضيل المصدر" GroupName="Strategy" IsChecked="True"
                             Foreground="#87CEEB" Margin="0,0,16,0" Click="Strategy_PreferSource"/>
                <RadioButton Content="تفضيل الهدف" GroupName="Strategy"
                             Foreground="#FFD700" Margin="0,0,16,0" Click="Strategy_PreferTarget"/>
                <RadioButton Content="دمج ذكي" GroupName="Strategy"
                             Foreground="#00CC66" Click="Strategy_Smart"/>
            </StackPanel>
        </Border>

        <!-- Fields Grid: Source | Field | Target -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Fields}" AutoGenerateColumns="False"
                  Background="#1A1A2A" BorderBrush="#333348" BorderThickness="1"
                  RowBackground="#1A1A2A" AlternatingRowBackground="#222238"
                  Foreground="White" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#2A2A3A"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  HeadersVisibility="Column">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#252538"/>
                    <Setter Property="Foreground" Value="#AAAAAA"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderBrush" Value="#333348"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="الحقل" Binding="{Binding FieldName}" Width="150" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#CCCCCC"/>
                            <Setter Property="Padding" Value="6,4"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="المصدر" Binding="{Binding SourceValue}" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#87CEEB"/>
                            <Setter Property="Padding" Value="6,4"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="الهدف" Binding="{Binding TargetValue}" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FFD700"/>
                            <Setter Property="Padding" Value="6,4"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridCheckBoxColumn Header="استخدم المصدر" Binding="{Binding UseSource, UpdateSourceTrigger=PropertyChanged}" Width="100">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Preview -->
        <Border Grid.Row="3" Background="#0D1117" CornerRadius="6" Padding="10" Margin="0,8,0,8" MaxHeight="120">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding MergePreview, Mode=OneWay}" FontFamily="Consolas" FontSize="11"
                           Foreground="#00FF88" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="إلغاء" Click="Cancel_Click" Width="80" Height="34"
                    Background="#3A3A4A" Foreground="White" BorderBrush="#555577" BorderThickness="1"
                    Cursor="Hand" Margin="0,0,8,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="6" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="🧬 تأكيد الدمج" Click="Confirm_Click" Width="140" Height="34"
                    Background="#00884C" Foreground="White" BorderBrush="#00CC66" BorderThickness="1"
                    FontWeight="Bold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="6" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
