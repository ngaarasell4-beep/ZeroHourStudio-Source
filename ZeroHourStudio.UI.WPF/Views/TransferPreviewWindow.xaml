<Window x:Class="ZeroHourStudio.UI.WPF.Views.TransferPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:ZeroHourStudio.UI.WPF.Converters"
        Title="معاينة النقل" Height="500" Width="700"
        Background="#1E1E1E" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <conv:ZeroToCollapsedConverter x:Key="ZeroToCollapsedConverter"/>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📋 معاينة عملية النقل"
                   FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="#252525" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="🎯 الوحدة المراد نقلها"
                                   Foreground="#0078D4" FontWeight="Bold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="الاسم:" Foreground="#888"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding UnitName}"
                                       Foreground="White" FontWeight="Bold"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="الفصيل الأصلي:" Foreground="#888" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SourceFaction}"
                                       Foreground="#FF6B00" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="عدد التبعيات:" Foreground="#888" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DependencyCount}"
                                       Foreground="#00CC66" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="ملخص التبعيات:" Foreground="#888" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DependencyBreakdown}"
                                       Foreground="#87CEEB" TextWrapping="Wrap" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="مفقود:" Foreground="#888" Margin="0,5,0,0"
                                       Visibility="{Binding MissingCount, Converter={StaticResource ZeroToCollapsedConverter}}"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding MissingCount}" Foreground="#FF6666" Margin="0,5,0,0"
                                       Visibility="{Binding MissingCount, Converter={StaticResource ZeroToCollapsedConverter}}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="#252525" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="🎯 الهدف"
                                   Foreground="#0078D4" FontWeight="Bold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="المود:" Foreground="#888"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TargetModName}"
                                       Foreground="White" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="الفصيل المستهدف:" Foreground="#888" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TargetFaction}"
                                       Foreground="#FF6B00" FontWeight="Bold" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="الـ Slot:" Foreground="#888" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,5,0,0">
                                <Run Text="الموقع " Foreground="White"/>
                                <Run Text="{Binding SlotNumber}" Foreground="#00CC66" FontWeight="Bold"/>
                                <Run Text=" في " Foreground="White"/>
                                <Run Text="{Binding CommandSetName}" Foreground="#888"/>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="#3A2A1A" BorderBrush="#FF6B00" BorderThickness="1"
                        CornerRadius="5" Padding="15" Margin="0,0,0,10"
                        Visibility="{Binding HasWarnings, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="⚠ تحذيرات" Foreground="#FF6B00" FontWeight="Bold" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding Warnings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="#FFB366"
                                               TextWrapping="Wrap" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="#1A3A2A" BorderBrush="#00CC66" BorderThickness="1"
                        CornerRadius="5" Padding="15"
                        Visibility="{Binding HasNotes, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="✓ ملاحظات" Foreground="#00CC66" FontWeight="Bold" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding Notes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="#66FFB3"
                                               TextWrapping="Wrap" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Background="#252525" CornerRadius="5" Padding="15" Margin="0,15,0,15">
            <TextBlock TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="11">
                <Run Text="💡 "/>
                <Run Text="{Binding Summary}"/>
            </TextBlock>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="إلغاء" Padding="20,8" Margin="0,0,10,0"
                    Background="#444" Foreground="White" Click="Cancel_Click"/>
            <Button Content="✓ متابعة النقل" Padding="20,8"
                    Background="#0078D4" Foreground="White" FontWeight="Bold"
                    Click="Proceed_Click"/>
        </StackPanel>
    </Grid>
</Window>
